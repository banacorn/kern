doctype 5
html ->
    head ->
        meta charset: 'utf-8'
        title 'redisev'
        link type: 'text/css', href: 'reset.css', rel: 'stylesheet'
        link type: 'text/css', href: 'style.css', rel: 'stylesheet'
        script src: 'lib/jquery-1.6.2.min.js'
        script src: 'script.js'
    body -> 
        header ->
            nav ->
                ul ->
                    li '.selected', ->
                        h1 ->
                            a href:'#', ->
                                text 'Redis'
                                span 'ev'
                    li ->
                        a href:'#install', -> 
                            'Install'
                    li ->
                        a href:'#tour', -> 
                            'Tour'
                    li ->
                        a href:'#doc', -> 
                            'Doc'
                    li ->
                        a href:'#blah', -> 
                            'Blah'
        article ->
            h2 '#', -> '"Just another Redis client for Node.js"'
            section ->
                p ->
                    img src:'images/trollface.jpg', alt:'trollface', ->
                    text 'Or really?'
        article ->
            h2 '#install', -> 'Install'
            section ->
                pre '''
                npm install redisev
                '''
        article ->
            h2 '#tour', -> 'Tour'
            section ->
        article ->
            h2 '#doc', -> 'Doc'
            section ->
        article ->
            h2 '#blah', -> 'Blah'
            section ->
                
            
                    
